package com.example.producr_service.application.strategy;

// Mục đích upload để application layer chọn đúng cấu hình lưu trữ.
public enum UploadPurpose {
    CATEGORY_IMAGE,
    PRODUCT_IMAGE,
    PRODUCT_VARIANT_IMAGE
}
